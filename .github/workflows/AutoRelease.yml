name: Auto Releases

on: 
  workflow_dispatch:
  schedule:
    - cron: "0 0 1 * *"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v6

    - uses: ncipollo/release-action@v1
      with:
        tag: 0.0.1
